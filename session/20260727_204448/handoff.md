# Handoff

## Resume From Here
CML-633D Task 9 A07 integration is complete locally on `feat/cml-633d-institutional-configuration`; no commit was created.

## Next Actions
- Review the A07 changes or commit them only if explicitly requested.

## Watch Outs
- Preserve unrelated dirty files.
- Use `--pool=threads --maxWorkers=1` for Vitest under Windows resource pressure.
- Final verification: full suite 996/996, TypeScript and build green, `git diff --check` clean except CRLF notices.
